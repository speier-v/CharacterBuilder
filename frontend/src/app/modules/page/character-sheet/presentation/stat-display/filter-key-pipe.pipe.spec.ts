import { FilterKeyPipePipe } from './filter-key-pipe.pipe';

describe('FilterKeyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterKeyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
